{
  "buildCommand": "pnpm install && pnpm --filter @workspace/noor-landing run build",
  "outputDirectory": "artifacts/noor-landing/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
