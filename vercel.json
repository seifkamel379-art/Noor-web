{
  "buildCommand": "pnpm install && pnpm --filter @workspace/noor-landing run build && cp -r artifacts/noor-landing/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
